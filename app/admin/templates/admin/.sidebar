.sidebar {
    width: 209px; /* Ширина сайдбара */
    background: #FFFFFF;
    border-radius: 4px;
    box-shadow: 0px 0px 1px #171a1f, 0px 0px 2px #171a1f;
    overflow: auto; /* Для прокрутки если контент не помещается */
    height: 100vh; /* Полная высота видимой области */
  }
  /* Общие стили для меню сайдбара */
  .sidebar-menu {
    top: 51px;
    left: 0px;
    width: 156px;
    height: 325px;
    display: flex;
    flex-direction: column;
    font-family: Manrope, sans-serif;
    font-size: 14px;
    font-weight: 400;
    line-height: 22px;
    opacity: 1;
    gap: 17px;
  }
  
  /* Стили для каждого пункта в меню сайдбара */
  .sidebar-menu .sidebar-menu-item {
    padding: 16px;
    display: flex;
    align-items: center;
    justify-content: left;
    color: #565D6D;
    background: #000000;
    border-radius: 8px;
    gap: 8px;
    cursor: pointer;
    white-space: nowrap;
  }
  
  /* Отступ для вложенных пунктов меню сайдбара */
  .sidebar-menu .sidebar-menu-item.indent {
    padding-left: 31px;
  }
  
  /* Стили для выбранного пункта меню сайдбара */
  .sidebar-menu .sidebar-menu-item.selected {
    position: relative;
    font-weight: 700;
    color: #6D31ED;
    background: #D3C1FA;
  }
  
  /* Стили для иконок в меню сайдбара */
  .sidebar-menu svg.icon {
    width: 24px;
    height: 24px;
    fill: currentColor;
  }
  
  /* Стили для выпадающего списка */
  .dropdown {
    position: relative;
    width: 100%;
    margin-bottom: 20px; /* Отступ снизу для выпадающего списка */
  }
  
  .dropdown select {
    width: 100%;
    height: 36px;
    padding: 0 12px;
    font-family: Manrope, sans-serif;
    font-size: 14px;
    line-height: 22px;
    color: #171A1F;
    background: #FFFFFF;
    border-radius: 4px;
    border: 1px solid #9095A1;
    appearance: none; /* Убираем стандартное оформление */
    cursor: pointer;
  }
  
  .dropdown select:hover {
    border-color: #6F7787;
  }
  
  .dropdown::after {
    content: '';
    position: absolute;
    top: 50%;
    right: 12px;
    transform: translateY(-50%);
    width: 16px;
    height: 16px;
    background: url('path/to/your/icon.svg') no-repeat center; /* Путь к вашей иконке */
    pointer-events: none; /* Исключаем иконку из событий курсора */
  }
  
  /* Стили для элемента select при фокусе и при отключении */
  .dropdown select:focus {
    border-color: #9095A1;
  }
  
  .dropdown select:disabled {
    background: #ECECEC;
  }
  
  /* Стили для иконки внутри select при отключении */
  .dropdown select:disabled + .icon,
  .dropdown select:disabled + .icon + .icon {
    fill: #9095A1;
  }
  
  